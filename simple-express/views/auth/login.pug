extends ../_layouts/master

block content 
    h1 登入表單
    form.form(id = "loginFrm", method="POST", action="/auth/login")
        div 
            label(for="email") 帳號:
            input#email(name = "email", type ="mail", value="chien313jay@gmail.com")
        div 
            label(for="password") 密碼:
            input#password(name="password", type="password", required, value="111")
        button(type="submit") 送出